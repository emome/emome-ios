language: objective-c
 
branches:
 only:
 - master
    
xcode_workspace: Emome.xcworkspace
xcode_scheme: EmomeTests
osx_image: xcode7
xcode_sdk: iphonesimulator9.0

script:
- xcodebuild clean build test -workspace Emome.xcworkspace -scheme EmomeTests -sdk iphonesimulator9.0 dONLY_ACTIVE_ARCH=NO
