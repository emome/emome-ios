language: objective-c
 
branches:
 only:
 - master
    
xcode_workspace: Emome.xcworkspace
xcode_scheme: Emome 
osx_image: xcode7
xcode_sdk: iphonesimulator9.0

script:
- xctool clean build test -workspace Emome.xcworkspace -scheme Emome -sdk iphonesimulator9.0 ONLY_ACTIVE_ARCH=NO
